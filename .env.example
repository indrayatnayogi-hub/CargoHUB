# Copy to .env for local tooling, or configure these as GitHub Pages/Actions secrets.
# Note: browser apps should use Supabase anon keys protected by Row Level Security policies.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-public-anon-key
